'use strict';

const STATUS={EXPLORAR:'A EXPLORAR',CORTE:'CORTADA',NAO:'CORTE NÃO EFETUADO',ARRASTE:'ARRASTE',ROMANEADA:'ROMANEADA',SEM:'SEM SITUAÇÃO'};
const STATUS_COLOR={[STATUS.EXPLORAR]:'#35d35f',[STATUS.CORTE]:'#ff404d',[STATUS.NAO]:'#ff8a19',[STATUS.ARRASTE]:'#8b5cf6',[STATUS.ROMANEADA]:'#2693ff',[STATUS.SEM]:'#98a49d'};
const CLOUD_INTERVAL_MS=60000, FILE_WATCH_INTERVAL_MS=30000, PAGE_SIZE=100;
const MAP_BOUNDS={west:-56.18747860,east:-56.14036214,north:-3.24108705,south:-3.30725537};
const ALIASES={
  ut:['N UT','Nº UT','N° UT','UT','NR UT','NUMERO UT','NÚMERO UT'],faixa:['FAIXA','N FAIXA','Nº FAIXA','N° FAIXA'],
  arvore:['ARVORE','ÁRVORE','N ARVORE','Nº ARVORE','N° ARVORE','NR ARVORE','NR. ARVORE','NUMERO','NÚMERO','N ARV','Nº ARV'],
  nome:['NOME COMUM','NOME','ESPECIE','ESPÉCIE'],cap:['CAP (CM)','CAP(CM)','CAP CM','CAP'],h:['H (M)','H(M)','H M','ALTURA','ALTURA (M)'],
  lat:['LATITUDE','LAT'],lon:['LONGITUDE','LON','LONG'],motosserrista:['MOTOSSERRISTA CORTE','MOTOSERRISTA','MOTO SERRISTA CORTE','MOTOSSERRISTA'],
  dataCorte:['DATA DO CORTE','DATA CORTE','DATA DE CORTE','DT CORTE','DT. CORTE','DATA'],upa:['UPA'],romaneador:['ROMANEADOR','ROMANEADOR '],
  motivoNao:['MOTIVO','OBS','OBSERVACAO','OBSERVAÇÃO','CORTE NAO EFETUADO','CORTE NÃO EFETUADO'],situacaoBase:['SITUACAO','SITUAÇÃO','STATUS','SITUACAO ARVORE','SITUAÇÃO ÁRVORE','CATEGORIA'],arrasteFlag:['ARRASTE','ARRASTADA','ARRASTADAS','DATA ARRASTE','DATA DO ARRASTE','STATUS ARRASTE','SITUACAO ARRASTE','SITUAÇÃO ARRASTE']
};
let allTrees=[],filteredTrees=[],currentPage=1,currentUpdatedAt=null,currentSource='',cloudTimer=null,fileWatchTimer=null,watchedFileHandle=null,watchedLastModified=0,activeScreen='home';
let navTarget=null,navWatchId=null,currentGps=null,selectedTree=null;
const $=id=>document.getElementById(id);
window.__onNativeLocation=function(lat,lon,accuracy){currentGps={lat:Number(lat),lon:Number(lon),accuracy:Number(accuracy)||0};if(navTarget)updateNavigationFromGps(currentGps);else drawMapSoon()};

function norm(v){return String(v??'').normalize('NFD').replace(/[\u0300-\u036f]/g,'').toUpperCase().replace(/[º°]/g,'').replace(/[^A-Z0-9]+/g,' ').trim()}
function compact(v){return norm(v).replace(/\s+/g,' ')}
function cleanNumKey(v){if(v===null||v===undefined||v==='')return'';return norm(String(v).trim().replace(/\.0+$/,'')).replace(/\s/g,'')}
function firstNonEmpty(...vals){return vals.find(v=>v!==undefined&&v!==null&&String(v).trim()!=='')??''}
function formatVal(v){return(v===undefined||v===null||String(v).trim()==='')?'-':String(v).trim()}
function getField(row,key){const aliases=(ALIASES[key]||[]).map(compact);for(const[k,v]of Object.entries(row||{}))if(aliases.includes(compact(k)))return v;for(const[k,v]of Object.entries(row||{})){const nk=compact(k);if(aliases.some(a=>nk===a||nk.startsWith(a+' ')||a.startsWith(nk+' ')))return v}return''}
function coord(v){if(typeof v==='number')return Number.isFinite(v)?v:null;if(v===null||v===undefined)return null;let s=String(v).trim().replace(/\s/g,'');if(!s)return null;if(s.includes(',')&&s.includes('.')){if(s.lastIndexOf(',')>s.lastIndexOf('.'))s=s.replace(/\./g,'').replace(',','.');else s=s.replace(/,/g,'')}else if(s.includes(','))s=s.replace(',','.');const n=Number(s);return Number.isFinite(n)?n:null}
function numberVal(v){if(typeof v==='number')return Number.isFinite(v)?v:0;let s=String(v??'').trim();if(!s)return 0;s=s.replace(/\s/g,'');if(s.includes(',')&&s.includes('.')){if(s.lastIndexOf(',')>s.lastIndexOf('.'))s=s.replace(/\./g,'').replace(',','.');else s=s.replace(/,/g,'')}else if(s.includes(','))s=s.replace(',','.');const n=Number(s);return Number.isFinite(n)?n:0}
function validCoord(lat,lon){return lat!==null&&lon!==null&&Math.abs(lat)<=90&&Math.abs(lon)<=180&&!(lat===0&&lon===0)}
function utNumber(v){const s=cleanNumKey(v),m=s.match(/\d+/);if(!m)return null;const n=parseInt(m[0],10);return Number.isFinite(n)?n:null}
function utKey(v){const n=utNumber(v);return n!==null?String(n):cleanNumKey(v)}
function isTargetUt(v){const n=utNumber(v);return n!==null&&n>=1&&n<=19}
function treeKey(row){const ut=utKey(getField(row,'ut')),arv=cleanNumKey(getField(row,'arvore'));return ut&&arv?`${ut}|${arv}`:''}
function escapeHtml(s){return String(s??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]))}
function formatCoord(v){return typeof v==='number'&&Number.isFinite(v)?v.toFixed(7):'-'}
function formatDateValue(v){if(!v)return'';if(v instanceof Date&&!Number.isNaN(v.getTime()))return v.toLocaleDateString('pt-BR');if(typeof v==='number'&&window.XLSX?.SSF){try{const d=XLSX.SSF.parse_date_code(v);if(d)return `${String(d.d).padStart(2,'0')}/${String(d.m).padStart(2,'0')}/${d.y}`}catch{}}const s=String(v).trim();if(!s)return'';const d=new Date(s);if(!Number.isNaN(d.getTime())&&/[-/]/.test(s))return d.toLocaleDateString('pt-BR');return s}
function baseClassification(row){const preferred=norm(getField(row,'situacaoBase'));const vals=[preferred,...Object.values(row||{}).map(norm)].filter(Boolean);if(vals.some(v=>v==='SUBSTITUTA'||v.includes('SUBSTITUTA')))return'SUBSTITUTA';if(vals.some(v=>v==='REMANESCENTE'||v.includes('REMANESCENTE')))return'REMANESCENTE';if(vals.some(v=>v==='EXPLORAR CAP'||v==='EXPLORAR_CAP'||v.includes('EXPLORAR CAP')))return'EXPLORAR_CAP';if(vals.some(v=>v==='EXPLORAR'))return'EXPLORAR';return preferred||''}
function baseExploreType(row){const c=baseClassification(row);return c==='EXPLORAR_CAP'?'EXPLORAR_CAP':c==='EXPLORAR'?'EXPLORAR':''}
function classLabel(v){const n=norm(v);if(n==='EXPLORAR CAP'||n==='EXPLORAR_CAP')return'Explorar_CAP';if(n==='EXPLORAR')return'Explorar';if(n==='SUBSTITUTA')return'Substituta';if(n==='REMANESCENTE')return'Remanescente';return String(v||'').trim()||'Sem classificação'}
function truthyArraste(v){const n=norm(v);return !!n&&!['NAO','N','0','FALSE','FALSO','NA'].includes(n)}
function showToast(msg){const t=$('toast');t.textContent=msg;t.hidden=false;clearTimeout(t._tm);t._tm=setTimeout(()=>t.hidden=true,3500)}
function updateNetwork(){const on=navigator.onLine,p=$('networkPill');p.classList.toggle('offline',!on);p.querySelector('span').textContent=on?'Online':'Offline'}

function guessHeaderRow(matrix){let best=0,bestScore=-1;const targets=[...ALIASES.ut,...ALIASES.faixa,...ALIASES.arvore,...ALIASES.nome,...ALIASES.lat,...ALIASES.lon].map(compact);for(let r=0;r<Math.min(matrix.length,40);r++){const cells=matrix[r].map(compact);let score=0;for(const c of cells)if(targets.some(t=>c===t||c.includes(t)||t.includes(c)))score++;if(score>bestScore){bestScore=score;best=r}}return best}
function sheetToObjects(sheet){const matrix=XLSX.utils.sheet_to_json(sheet,{header:1,defval:'',raw:true,blankrows:false});if(!matrix.length)return[];const hr=guessHeaderRow(matrix),headers=matrix[hr].map((h,i)=>String(h??'').trim()||`COL_${i+1}`),out=[];for(let i=hr+1;i<matrix.length;i++){const arr=matrix[i];if(!arr||arr.every(v=>String(v??'').trim()===''))continue;const o={};headers.forEach((h,j)=>o[h]=arr[j]??'');out.push(o)}return out}
function findSheetName(wb,kind){const nn=wb.SheetNames.map(raw=>({raw,n:norm(raw)}));if(kind==='nao')return(nn.find(x=>x.n.includes('CORTE')&&x.n.includes('NAO'))||{}).raw;if(kind==='rom')return(nn.find(x=>x.n.includes('ROMANE'))||{}).raw;if(kind==='arraste')return(nn.find(x=>x.n.includes('ARRASTAD')||x.n.includes('ARRASTE'))||{}).raw;if(kind==='corte')return(nn.find(x=>x.n==='CORTE')||nn.find(x=>x.n.includes('CORTE')&&!x.n.includes('NAO'))||{}).raw;if(kind==='base')return(nn.find(x=>x.n.includes('IF100'))||nn.find(x=>x.n.includes('INVENT'))||{}).raw||wb.SheetNames[0];return null}
function indexByTree(rows){const m=new Map();for(const r of rows){const k=treeKey(r);if(k&&!m.has(k))m.set(k,r)}return m}
function ensureXlsx(){return new Promise((resolve,reject)=>{if(window.XLSX)return resolve();if(!navigator.onLine)return reject(new Error('Para importar uma planilha pela primeira vez, conecte o computador à internet. Depois os dados ficam salvos para uso offline.'));showToast('Carregando leitor de Excel...');const s=document.createElement('script');s.src='https://cdn.jsdelivr.net/npm/xlsx@0.18.5/dist/xlsx.full.min.js';s.onload=()=>resolve();s.onerror=()=>reject(new Error('Não foi possível carregar o leitor de Excel. Verifique a internet.'));document.head.appendChild(s)})}
async function processWorkbook(file){await ensureXlsx();showToast('Lendo a planilha...');const data=await file.arrayBuffer();try{await dbPut('lastWorkbookBlob',new Blob([data],{type:file.type||'application/octet-stream'}));await dbPut('lastWorkbookMeta',{name:file.name||'planilha',lastModified:file.lastModified||Date.now(),size:file.size||data.byteLength})}catch(e){console.warn('Não foi possível guardar o arquivo bruto',e)}const wb=XLSX.read(data,{type:'array',cellDates:true});const baseName=findSheetName(wb,'base'),corteName=findSheetName(wb,'corte'),naoName=findSheetName(wb,'nao'),arrasteName=findSheetName(wb,'arraste'),romName=findSheetName(wb,'rom');if(!baseName)throw new Error('Não encontrei a aba principal.');const base=sheetToObjects(wb.Sheets[baseName]),corte=corteName?sheetToObjects(wb.Sheets[corteName]):[],nao=naoName?sheetToObjects(wb.Sheets[naoName]):[],arraste=arrasteName?sheetToObjects(wb.Sheets[arrasteName]):[],rom=romName?sheetToObjects(wb.Sheets[romName]):[];const corteMap=indexByTree(corte),naoMap=indexByTree(nao),arrasteMap=indexByTree(arraste),romMap=indexByTree(rom);const result=[];let foraUt=0,semCoord=0;for(const r of base){if(!isTargetUt(getField(r,'ut'))){foraUt++;continue}const key=treeKey(r);if(!key)continue;const cr=corteMap.get(key),nr=naoMap.get(key),ar=arrasteMap.get(key),rr=romMap.get(key),classification=baseClassification(r),baseType=baseExploreType(r);const arrasteFlag=truthyArraste(firstNonEmpty(ar&&getField(ar,'arrasteFlag'),cr&&getField(cr,'arrasteFlag'),r&&getField(r,'arrasteFlag')));let status=(baseType?STATUS.EXPLORAR:STATUS.SEM);if(nr)status=STATUS.NAO;if(cr)status=STATUS.CORTE;if(ar||arrasteFlag)status=STATUS.ARRASTE;if(rr)status=STATUS.ROMANEADA;const lat=coord(getField(r,'lat')),lon=coord(getField(r,'lon'));if(!validCoord(lat,lon))semCoord++;result.push({key,upa:firstNonEmpty(getField(r,'upa')),ut:firstNonEmpty(getField(r,'ut')),faixa:firstNonEmpty(getField(r,'faixa')),arvore:firstNonEmpty(getField(r,'arvore')),nome:firstNonEmpty(getField(r,'nome')),cap:firstNonEmpty(getField(r,'cap')),capNum:numberVal(getField(r,'cap')),h:firstNonEmpty(getField(r,'h')),latitude:lat,longitude:lon,status,classificacao:classLabel(classification),baseExplore:!!baseType,baseExploreType:baseType,motosserrista:firstNonEmpty(cr&&getField(cr,'motosserrista'),getField(r,'motosserrista')),dataCorte:formatDateValue(firstNonEmpty(cr&&getField(cr,'dataCorte'),getField(r,'dataCorte'))),romaneador:firstNonEmpty(rr&&getField(rr,'romaneador')),motivoNao:firstNonEmpty(nr&&getField(nr,'motivoNao'))})}currentUpdatedAt=new Date().toISOString();currentSource=`${file.name} • UT 01–19 • ${[baseName,corteName,naoName,arrasteName,romName].filter(Boolean).join(' + ')}`;await setData(result,currentUpdatedAt,currentSource,true);await saveLocal({schemaVersion:6,updatedAt:currentUpdatedAt,source:currentSource,data:result});const cfg=getSettings();if(cfg.apiUrl&&cfg.syncKey)await uploadCloud();const coordOk=result.length-semCoord;showToast(`Atualizado: ${result.length.toLocaleString('pt-BR')} árvores das UTs 01–19 • ${coordOk.toLocaleString('pt-BR')} com coordenadas.`)}

async function setData(data,updatedAt,source,reset=false){allTrees=(Array.isArray(data)?data:[]).filter(t=>isTargetUt(t.ut));currentUpdatedAt=updatedAt||null;currentSource=source||'';if(reset)currentPage=1;populateFilters();applyFilters();updateSyncStatus();renderHomeSearch();drawMapSoon();updateLastWorkbookInfo()}
function updateSyncStatus(){if(!allTrees.length){$('syncStatus').textContent='Sem dados carregados';return}const d=currentUpdatedAt?new Date(currentUpdatedAt):null,stamp=d&&!Number.isNaN(d.getTime())?d.toLocaleString('pt-BR'):'agora';$('syncStatus').textContent=`${allTrees.length.toLocaleString('pt-BR')} árvores • atualizado ${stamp}`}
function populateFilters(){const add=(id,values,label)=>{const s=$(id),cur=s.value,vals=[...new Set(values.map(v=>String(v??'').trim()).filter(Boolean))].sort((a,b)=>a.localeCompare(b,'pt-BR',{numeric:true}));s.innerHTML=`<option value="">${label}</option>`+vals.map(v=>`<option>${escapeHtml(v)}</option>`).join('');if(vals.includes(cur))s.value=cur};add('utFilter',allTrees.map(x=>x.ut),'Todas');add('faixaFilter',allTrees.map(x=>x.faixa),'Todas');add('motoFilter',allTrees.map(x=>x.motosserrista),'Todos')}
function currentFilters(){return{q:norm($('searchInput').value),ut:$('utFilter').value,faixa:$('faixaFilter').value,status:$('statusFilter').value,moto:$('motoFilter').value}}
function applyFilters(){const f=currentFilters();document.querySelectorAll('#statusChips button').forEach(b=>b.classList.toggle('active',b.dataset.status===f.status));filteredTrees=allTrees.filter(t=>{if(f.ut&&String(t.ut)!==f.ut)return false;if(f.faixa&&String(t.faixa)!==f.faixa)return false;if(f.status&&t.status!==f.status)return false;if(f.moto&&String(t.motosserrista)!==f.moto)return false;if(f.q&&!norm([t.ut,t.faixa,t.arvore,t.nome,t.motosserrista,t.status,t.upa,t.classificacao].join(' ')).includes(f.q))return false;return true});const pages=Math.max(1,Math.ceil(filteredTrees.length/PAGE_SIZE));if(currentPage>pages)currentPage=pages;renderKpis();renderTreeList();renderFilterCount();drawMapSoon()}
function renderKpis(){const counts={};Object.values(STATUS).forEach(s=>counts[s]=0);allTrees.forEach(t=>counts[t.status]=(counts[t.status]||0)+1);const explorar=allTrees.filter(t=>t.status===STATUS.EXPLORAR),cap=explorar.reduce((a,t)=>a+(Number(t.capNum)||numberVal(t.cap)),0);$('kpiTotal').textContent=allTrees.length.toLocaleString('pt-BR');$('kpiExplorar').textContent=explorar.length.toLocaleString('pt-BR');$('kpiExplorarCap').textContent=`CAP: ${cap.toLocaleString('pt-BR',{maximumFractionDigits:1})}`;$('kpiCorte').textContent=(counts[STATUS.CORTE]||0).toLocaleString('pt-BR');$('kpiNao').textContent=(counts[STATUS.NAO]||0).toLocaleString('pt-BR');$('kpiArraste').textContent=(counts[STATUS.ARRASTE]||0).toLocaleString('pt-BR');$('kpiRomaneada').textContent=(counts[STATUS.ROMANEADA]||0).toLocaleString('pt-BR');[['mkpiTotal',allTrees.length],['mkpiExplorar',explorar.length],['mkpiCorte',counts[STATUS.CORTE]||0],['mkpiNao',counts[STATUS.NAO]||0],['mkpiArraste',counts[STATUS.ARRASTE]||0],['mkpiRomaneada',counts[STATUS.ROMANEADA]||0]].forEach(([id,v])=>{const e=$(id);if(e)e.textContent=Number(v||0).toLocaleString('pt-BR')})}
function renderTreeList(){const pages=Math.max(1,Math.ceil(filteredTrees.length/PAGE_SIZE)),start=(currentPage-1)*PAGE_SIZE,slice=filteredTrees.slice(start,start+PAGE_SIZE);$('resultCount').textContent=`${filteredTrees.length.toLocaleString('pt-BR')} árvores`;$('pageInfo').textContent=`${currentPage} / ${pages}`;$('prevPage').disabled=currentPage<=1;$('nextPage').disabled=currentPage>=pages;$('treeList').innerHTML=slice.map((t,i)=>`<button class="tree-row" data-tree="${start+i}"><i class="status-dot" style="background:${STATUS_COLOR[t.status]||STATUS_COLOR[STATUS.SEM]}"></i><span class="tree-main"><strong>Árvore Nº ${escapeHtml(formatVal(t.arvore))}</strong><b>UT ${escapeHtml(formatVal(t.ut))} • Faixa ${escapeHtml(formatVal(t.faixa))}</b><small>${escapeHtml(formatVal(t.nome))} • ${escapeHtml(formatVal(t.classificacao))}</small></span><span class="tree-side"><span>${escapeHtml(t.status)}</span><i>›</i></span></button>`).join('')||'<div class="offline-info"><b>!</b><div><strong>Nenhuma árvore encontrada</strong><small>Altere a pesquisa ou os filtros.</small></div></div>';document.querySelectorAll('[data-tree]').forEach(b=>b.onclick=()=>showTree(filteredTrees[Number(b.dataset.tree)]))}
function renderFilterCount(){const f=currentFilters(),n=[f.ut,f.faixa,f.status,f.moto].filter(Boolean).length;$('filterCount').hidden=!n;$('filterCount').textContent=n}
function showTree(t){if(!t)return;$('treeStatus').querySelector('i').style.background=STATUS_COLOR[t.status]||STATUS_COLOR[STATUS.SEM];$('treeStatus').querySelector('span').textContent=t.status;$('treeDialogTitle').textContent=`Árvore Nº ${formatVal(t.arvore)}`;$('treeSubtitle').textContent=`UT ${formatVal(t.ut)} • Faixa ${formatVal(t.faixa)} • ${formatVal(t.nome)}`;const fields=[['Classificação',t.classificacao],['Nome comum',t.nome],['CAP (cm)',t.cap],['H (m)',t.h],['Motosserrista Corte',t.motosserrista],['Data do corte',t.dataCorte],['Latitude',formatCoord(t.latitude)],['Longitude',formatCoord(t.longitude)],['Motivo não efetuado',t.motivoNao],['Romaneador',t.romaneador]];$('treeDetails').innerHTML=fields.map(([a,b])=>`<div class="detail"><span>${escapeHtml(a)}</span><strong>${escapeHtml(formatVal(b))}</strong></div>`).join('');const has=validCoord(t.latitude,t.longitude);$('treeActions').innerHTML=has?`<button class="nav-offline" id="navOfflineBtn">⌖ Navegar offline</button><button class="maps-online" id="viewMapBtn">Ver no mapa</button><button class="maps-online" id="mapsBtn">Google Maps</button><button class="waze-online" id="wazeBtn">Waze</button>`:'<div class="offline-info"><b>!</b><div><strong>Sem coordenada válida</strong><small>Esta árvore não possui latitude/longitude válida.</small></div></div>';if(has){$('navOfflineBtn').onclick=()=>{ $('treeDialog').close();startOfflineNavigation(t)};$('viewMapBtn').onclick=()=>{$('treeDialog').close();focusTreeOnMap(t)};$('mapsBtn').onclick=()=>openExternal(`https://www.google.com/maps/dir/?api=1&destination=${t.latitude},${t.longitude}`);$('wazeBtn').onclick=()=>openExternal(`https://waze.com/ul?ll=${t.latitude}%2C${t.longitude}&navigate=yes`)}$('treeDialog').showModal()}
function homeSearchMatches(q){const s=cleanNumKey(q);if(!s)return[];let exact=allTrees.filter(t=>cleanNumKey(t.arvore)===s);if(exact.length)return exact.slice(0,12);return allTrees.filter(t=>cleanNumKey(t.arvore).includes(s)).slice(0,12)}
function renderHomeSearch(){const box=$('homeSearchResults'),input=$('homeTreeSearch');if(!box||!input)return;const q=input.value.trim();$('homeTreeClear').hidden=!q;if(!q){box.innerHTML='<div class="home-search-empty"><b>Digite o número da árvore</b><small>O app procura nas UTs 01 a 19 e mostra a classificação e a situação atual.</small></div>';return}if(!allTrees.length){box.innerHTML='<div class="home-search-empty"><b>Carregue a planilha primeiro</b><small>Use “Atualizar planilha” para importar os dados do inventário.</small></div>';return}const hits=homeSearchMatches(q);if(!hits.length){box.innerHTML='<div class="home-search-empty"><b>Nenhuma árvore encontrada</b><small>Confira a numeração digitada.</small></div>';return}box.innerHTML=hits.map((t,i)=>`<button class="home-result-row" data-home-tree="${i}"><i class="result-dot" style="background:${STATUS_COLOR[t.status]||STATUS_COLOR[STATUS.SEM]}"></i><span class="home-result-main"><strong>Árvore Nº ${escapeHtml(formatVal(t.arvore))}</strong><b>UT ${escapeHtml(formatVal(t.ut))} • Faixa ${escapeHtml(formatVal(t.faixa))}</b><small>${escapeHtml(formatVal(t.nome))}</small></span><span class="home-result-side"><span class="class-badge">${escapeHtml(formatVal(t.classificacao))}</span><span class="status-badge" style="background:${STATUS_COLOR[t.status]||STATUS_COLOR[STATUS.SEM]}">${escapeHtml(t.status)}</span></span></button>`).join('');box.querySelectorAll('[data-home-tree]').forEach(b=>b.onclick=()=>showTree(hits[Number(b.dataset.homeTree)]))}
function findAndOpenMapTree(){const input=$('mapTreeSearch');if(!input)return;const q=input.value.trim();if(!q){showToast('Digite o número da árvore.');input.focus();return}const hits=homeSearchMatches(q),exact=hits.filter(t=>cleanNumKey(t.arvore)===cleanNumKey(q));if(!hits.length){showToast('Árvore não encontrada nas UTs 01 a 19.');return}if(exact.length>1){goScreen('trees');$('searchInput').value=q;$('clearSearch').hidden=false;currentPage=1;applyFilters();showToast(`${exact.length} árvores com esse número. Escolha pela UT e faixa.`);return}const t=(exact[0]||hits[0]);selectedTree=t;focusTreeOnMap(t);setTimeout(()=>showTree(t),220)}
function focusTreeOnMap(t){if(!t||!validCoord(t.latitude,t.longitude)){showToast('Esta árvore não possui latitude/longitude válida.');return}selectedTree=t;goScreen('map');setTimeout(()=>{if(!mapState.imgReady)return;const c=mapState.canvas,p=geoToImage(t.latitude,t.longitude);const targetScale=Math.max(mapState.minScale*5,Math.min(mapState.maxScale,mapState.scale));mapState.scale=targetScale;mapState.offsetX=c.width/2-p.x*targetScale;mapState.offsetY=c.height/2-p.y*targetScale;drawMapSoon()},80)}
function openExternal(url){if(!navigator.onLine){showToast('Sem internet. Use Navegar offline pelas coordenadas.');return}location.href=url}

let mapLabelsVisible=localStorage.getItem('nobre-map-labels')!=='0';
function isPlanTree(t){if(t?.baseExplore===true)return true;const c=norm(t?.classificacao||'');return c==='EXPLORAR'||c==='EXPLORAR CAP'||c==='EXPLORAR_CAP'}
function mapTrees(){return allTrees.filter(isPlanTree)}
function updateLabelsToggle(){const b=$('labelsToggleBtn');if(!b)return;b.classList.toggle('active',mapLabelsVisible);b.title=mapLabelsVisible?'Ocultar numeração':'Mostrar numeração';b.textContent='Nº'}
function toggleMapLabels(){mapLabelsVisible=!mapLabelsVisible;localStorage.setItem('nobre-map-labels',mapLabelsVisible?'1':'0');updateLabelsToggle();drawMapSoon();showToast(mapLabelsVisible?'Numeração ligada.':'Numeração ocultada.')}
const mapState={canvas:null,ctx:null,img:new Image(),imgReady:false,scale:1,offsetX:0,offsetY:0,minScale:.1,maxScale:16,dragging:false,lastX:0,lastY:0,moved:false,raf:0};
function initGeoMap(){mapState.canvas=$('geoMap');mapState.ctx=mapState.canvas.getContext('2d',{alpha:false});mapState.img.onload=()=>{mapState.imgReady=true;resizeMap(true)};mapState.img.src='mapa_upa13_bloco_norte.png';const c=mapState.canvas;c.addEventListener('pointerdown',e=>{c.setPointerCapture?.(e.pointerId);mapState.dragging=true;mapState.lastX=e.clientX;mapState.lastY=e.clientY;mapState.moved=false});c.addEventListener('pointermove',e=>{if(!mapState.dragging)return;const dx=e.clientX-mapState.lastX,dy=e.clientY-mapState.lastY;if(Math.hypot(dx,dy)>2)mapState.moved=true;mapState.offsetX+=dx;mapState.offsetY+=dy;mapState.lastX=e.clientX;mapState.lastY=e.clientY;drawMapSoon()});c.addEventListener('pointerup',e=>{mapState.dragging=false;if(!mapState.moved)handleMapTap(e)});c.addEventListener('pointercancel',()=>mapState.dragging=false);c.addEventListener('wheel',e=>{e.preventDefault();zoomMap(e.deltaY<0?1.18:.84,e.offsetX,e.offsetY)},{passive:false});window.addEventListener('resize',()=>resizeMap(false))}
function resizeMap(fit=false){const c=mapState.canvas;if(!c)return;const r=c.getBoundingClientRect(),dpr=Math.min(devicePixelRatio||1,2);c.width=Math.max(1,Math.round(r.width*dpr));c.height=Math.max(1,Math.round(r.height*dpr));c._dpr=dpr;if(fit)fitGeoMap();else drawMapSoon()}
function fitGeoMap(){if(!mapState.imgReady)return;const c=mapState.canvas,s=Math.min(c.width/mapState.img.width,c.height/mapState.img.height);mapState.scale=s;mapState.minScale=s*.65;mapState.maxScale=s*30;mapState.offsetX=(c.width-mapState.img.width*s)/2;mapState.offsetY=(c.height-mapState.img.height*s)/2;drawMapSoon()}
function zoomMap(factor,cx,cy){const c=mapState.canvas,dpr=c._dpr||1;cx=(cx??c.clientWidth/2)*dpr;cy=(cy??c.clientHeight/2)*dpr;const old=mapState.scale,next=Math.max(mapState.minScale,Math.min(mapState.maxScale,old*factor));if(next===old)return;const ix=(cx-mapState.offsetX)/old,iy=(cy-mapState.offsetY)/old;mapState.scale=next;mapState.offsetX=cx-ix*next;mapState.offsetY=cy-iy*next;drawMapSoon()}
function geoToImage(lat,lon){if(!mapState.imgReady)return null;const x=(lon-MAP_BOUNDS.west)/(MAP_BOUNDS.east-MAP_BOUNDS.west)*mapState.img.width,y=(MAP_BOUNDS.north-lat)/(MAP_BOUNDS.north-MAP_BOUNDS.south)*mapState.img.height;return{x,y}}
function imageToScreen(p){return{x:mapState.offsetX+p.x*mapState.scale,y:mapState.offsetY+p.y*mapState.scale}}
function insideBounds(t){return validCoord(t.latitude,t.longitude)&&t.longitude>=MAP_BOUNDS.west&&t.longitude<=MAP_BOUNDS.east&&t.latitude<=MAP_BOUNDS.north&&t.latitude>=MAP_BOUNDS.south}
function drawMapSoon(){if(mapState.raf)return;mapState.raf=requestAnimationFrame(()=>{mapState.raf=0;drawGeoMap()})}
function drawGeoMap(){const c=mapState.canvas,ctx=mapState.ctx;if(!c||!ctx||!mapState.imgReady)return;ctx.fillStyle='#f4f7f5';ctx.fillRect(0,0,c.width,c.height);ctx.imageSmoothingEnabled=true;ctx.drawImage(mapState.img,mapState.offsetX,mapState.offsetY,mapState.img.width*mapState.scale,mapState.img.height*mapState.scale);const pts=mapTrees(),mapped=pts.filter(insideBounds),counts={};for(const t of mapped)counts[t.status]=(counts[t.status]||0)+1;if($('mapCount'))$('mapCount').textContent=`${mapped.length.toLocaleString('pt-BR')} árvores do plano • ${(counts[STATUS.EXPLORAR]||0).toLocaleString('pt-BR')} A explorar`;const baseR=Math.max(1.6,Math.min(4.8,mapState.scale/mapState.minScale*1.55));const zr=mapState.scale/mapState.minScale;for(const t of pts){if(!insideBounds(t))continue;const p=imageToScreen(geoToImage(t.latitude,t.longitude));if(p.x<-8||p.x>c.width+8||p.y<-8||p.y>c.height+8)continue;ctx.beginPath();ctx.arc(p.x,p.y,baseR,0,Math.PI*2);ctx.fillStyle=STATUS_COLOR[t.status]||STATUS_COLOR[STATUS.SEM];ctx.fill();ctx.lineWidth=Math.max(1,c._dpr||1)*.55;ctx.strokeStyle='rgba(255,255,255,.9)';ctx.stroke();if(mapLabelsVisible&&zr>=(/Android/i.test(navigator.userAgent)?2.15:3.0)){ctx.font=`${Math.max(9,Math.min(15,9*zr/3.0))*(c._dpr||1)}px Arial`;ctx.textAlign='center';ctx.textBaseline='bottom';ctx.lineWidth=3*(c._dpr||1);ctx.strokeStyle='rgba(255,255,255,.96)';ctx.fillStyle='#063c25';const label=String(t.arvore??'');ctx.strokeText(label,p.x,p.y-baseR-2*(c._dpr||1));ctx.fillText(label,p.x,p.y-baseR-2*(c._dpr||1));}}if(selectedTree&&insideBounds(selectedTree)){const sp=imageToScreen(geoToImage(selectedTree.latitude,selectedTree.longitude));ctx.beginPath();ctx.arc(sp.x,sp.y,11*(c._dpr||1),0,Math.PI*2);ctx.strokeStyle='#ffdf38';ctx.lineWidth=4*(c._dpr||1);ctx.stroke();ctx.beginPath();ctx.arc(sp.x,sp.y,6*(c._dpr||1),0,Math.PI*2);ctx.fillStyle=STATUS_COLOR[selectedTree.status]||STATUS_COLOR[STATUS.SEM];ctx.fill();ctx.strokeStyle='#062f1e';ctx.lineWidth=2*(c._dpr||1);ctx.stroke();ctx.font=`${13*(c._dpr||1)}px Arial`;ctx.textAlign='center';ctx.textBaseline='bottom';ctx.lineWidth=4*(c._dpr||1);ctx.strokeStyle='rgba(255,255,255,.96)';ctx.fillStyle='#062f1e';ctx.strokeText(String(selectedTree.arvore??''),sp.x,sp.y-13*(c._dpr||1));ctx.fillText(String(selectedTree.arvore??''),sp.x,sp.y-13*(c._dpr||1));}if(navTarget&&insideBounds(navTarget)){const tp=imageToScreen(geoToImage(navTarget.latitude,navTarget.longitude));ctx.beginPath();ctx.arc(tp.x,tp.y,10*(c._dpr||1),0,Math.PI*2);ctx.strokeStyle='#062f1e';ctx.lineWidth=4*(c._dpr||1);ctx.stroke();ctx.beginPath();ctx.arc(tp.x,tp.y,6*(c._dpr||1),0,Math.PI*2);ctx.fillStyle='#ff3344';ctx.fill();if(currentGps&&insideGeo(currentGps.lat,currentGps.lon)){const gp=imageToScreen(geoToImage(currentGps.lat,currentGps.lon));ctx.beginPath();ctx.moveTo(gp.x,gp.y);ctx.lineTo(tp.x,tp.y);ctx.strokeStyle='#0d83ff';ctx.lineWidth=5*(c._dpr||1);ctx.globalAlpha=.82;ctx.stroke();ctx.globalAlpha=1;ctx.beginPath();ctx.arc(gp.x,gp.y,7*(c._dpr||1),0,Math.PI*2);ctx.fillStyle='#0876e8';ctx.fill();ctx.strokeStyle='#fff';ctx.lineWidth=3*(c._dpr||1);ctx.stroke()}}}
function insideGeo(lat,lon){return lon>=MAP_BOUNDS.west&&lon<=MAP_BOUNDS.east&&lat<=MAP_BOUNDS.north&&lat>=MAP_BOUNDS.south}
function handleMapTap(e){const c=mapState.canvas,r=c.getBoundingClientRect(),dpr=c._dpr||1,x=(e.clientX-r.left)*dpr,y=(e.clientY-r.top)*dpr;let best=null,bestD=14*dpr;const pts=mapTrees();for(const t of pts){if(!insideBounds(t))continue;const p=imageToScreen(geoToImage(t.latitude,t.longitude)),d=Math.hypot(p.x-x,p.y-y);if(d<bestD){bestD=d;best=t}}if(best)showTree(best)}
function focusOnTree(t,zoom=6){if(!insideBounds(t)){showToast('A coordenada está fora do mapa do Bloco Norte.');return}const c=mapState.canvas,p=geoToImage(t.latitude,t.longitude),target=Math.min(mapState.maxScale,mapState.minScale*zoom);mapState.scale=target;mapState.offsetX=c.width/2-p.x*target;mapState.offsetY=c.height/2-p.y*target;drawMapSoon()}
function centerGps(){if(!currentGps){requestGpsOnce();return}const fake={latitude:currentGps.lat,longitude:currentGps.lon};if(insideBounds(fake))focusOnTree(fake,8);else showToast('Sua posição atual está fora do mapa do Bloco Norte.')}
function updateNavigationFromGps(gps){if(!navTarget||!gps)return;const target={lat:navTarget.latitude,lon:navTarget.longitude},d=haversine(gps,target),b=bearing(gps,target);$('navDistance').textContent=d<1000?`${Math.round(d)} m`:`${(d/1000).toFixed(2)} km`;$('navBearing').textContent=`Rumo ${Math.round(b)}° ${cardinal(b)} • GPS ±${Math.round(gps.accuracy||0)} m`;$('navArrow').style.transform=`rotate(${b}deg)`;drawMapSoon()}
function requestGpsOnce(){if(window.AndroidBridge?.requestLocationOnce){AndroidBridge.requestLocationOnce();return}if(!navigator.geolocation){showToast('GPS não disponível neste aparelho.');return}navigator.geolocation.getCurrentPosition(p=>{currentGps={lat:p.coords.latitude,lon:p.coords.longitude,accuracy:p.coords.accuracy};centerGps();drawMapSoon()},e=>showToast('Não foi possível obter sua localização: '+e.message),{enableHighAccuracy:true,timeout:15000,maximumAge:3000})}
function haversine(a,b){const R=6371000,toRad=x=>x*Math.PI/180,dLat=toRad(b.lat-a.lat),dLon=toRad(b.lon-a.lon),la1=toRad(a.lat),la2=toRad(b.lat),h=Math.sin(dLat/2)**2+Math.cos(la1)*Math.cos(la2)*Math.sin(dLon/2)**2;return 2*R*Math.asin(Math.sqrt(h))}
function bearing(a,b){const toRad=x=>x*Math.PI/180,toDeg=x=>x*180/Math.PI,p1=toRad(a.lat),p2=toRad(b.lat),dl=toRad(b.lon-a.lon),y=Math.sin(dl)*Math.cos(p2),x=Math.cos(p1)*Math.sin(p2)-Math.sin(p1)*Math.cos(p2)*Math.cos(dl);return(toDeg(Math.atan2(y,x))+360)%360}
function cardinal(b){return['N','NE','L','SE','S','SO','O','NO'][Math.round(b/45)%8]}
function startOfflineNavigation(t){if(!validCoord(t.latitude,t.longitude)){showToast('Árvore sem coordenada válida.');return}navTarget=t;goScreen('map');setTimeout(()=>focusOnTree(t,7),120);$('navHud').hidden=false;$('mapHint').textContent=`Destino: Árvore ${formatVal(t.arvore)} • UT ${formatVal(t.ut)}`;if(navWatchId!==null&&navigator.geolocation){navigator.geolocation.clearWatch(navWatchId);navWatchId=null}if(window.AndroidBridge?.startLocationUpdates){AndroidBridge.startLocationUpdates();showToast('GPS offline ativado.');return}if(!navigator.geolocation){showToast('GPS não disponível.');return}navWatchId=navigator.geolocation.watchPosition(p=>{currentGps={lat:p.coords.latitude,lon:p.coords.longitude,accuracy:p.coords.accuracy};updateNavigationFromGps(currentGps)},e=>showToast('GPS: '+e.message),{enableHighAccuracy:true,maximumAge:1000,timeout:20000})}
function stopOfflineNavigation(){if(navWatchId!==null&&navigator.geolocation){navigator.geolocation.clearWatch(navWatchId);navWatchId=null}if(window.AndroidBridge?.stopLocationUpdates)AndroidBridge.stopLocationUpdates();navTarget=null;$('navHud').hidden=true;$('mapHint').textContent='Árvores do plano • cores pela situação • arraste para mover';drawMapSoon()}

function goScreen(name){activeScreen=name;document.querySelectorAll('.screen').forEach(s=>s.classList.remove('active'));document.querySelectorAll('.bottom-nav [data-go]').forEach(b=>b.classList.toggle('active',b.dataset.go===name));const map={home:'homeScreen',map:'mapScreen',trees:'treesScreen'};$(map[name]||'homeScreen').classList.add('active');if(name==='map')setTimeout(()=>resizeMap(true),60);if(name==='trees')renderTreeList()}
function openFilters(){renderFilterCount();$('filterDialog').showModal()}
function clearFilters(){['utFilter','faixaFilter','statusFilter','motoFilter'].forEach(id=>$(id).value='');currentPage=1;applyFilters()}

async function updateLastWorkbookInfo(){const e=$('lastWorkbookInfo');if(!e)return;try{const m=await dbGet('lastWorkbookMeta');if(m?.name){const d=m.lastModified?new Date(m.lastModified):null;const when=d&&!Number.isNaN(d.getTime())?d.toLocaleString('pt-BR'):'';e.textContent=`${m.name}${when?' • '+when:''}`;return}}catch{}if(currentSource)e.textContent=currentSource.split(' • ')[0]+' • dados salvos';else e.textContent='Nenhuma planilha importada ainda'}
function getSettings(){try{return JSON.parse(localStorage.getItem('nobre-inventario-settings')||'{}')}catch{return{}}}
function saveSettings(){const cfg={apiUrl:$('apiUrl').value.trim(),syncKey:$('syncKey').value.trim()};localStorage.setItem('nobre-inventario-settings',JSON.stringify(cfg));$('settingsDialog').close();startCloudTimer();showToast('Configuração salva.');if(cfg.apiUrl&&cfg.syncKey&&navigator.onLine)refreshCloud(false)}
function openSettings(){const c=getSettings();$('apiUrl').value=c.apiUrl||'';$('syncKey').value=c.syncKey||'';$('settingsDialog').showModal()}
function openDb(){return new Promise((resolve,reject)=>{const r=indexedDB.open('NobreInventarioGeo',2);r.onupgradeneeded=()=>{if(!r.result.objectStoreNames.contains('cache'))r.result.createObjectStore('cache')};r.onsuccess=()=>resolve(r.result);r.onerror=()=>reject(r.error)})}
async function saveLocal(payload){try{const db=await openDb();await new Promise((res,rej)=>{const tx=db.transaction('cache','readwrite');tx.objectStore('cache').put(payload,'dataset');tx.oncomplete=res;tx.onerror=()=>rej(tx.error)});db.close()}catch(e){console.warn(e)}}
async function loadLocal(){try{const db=await openDb();const v=await new Promise((res,rej)=>{const tx=db.transaction('cache','readonly'),r=tx.objectStore('cache').get('dataset');r.onsuccess=()=>res(r.result);r.onerror=()=>rej(r.error)});db.close();return v}catch{return null}}
async function dbPut(key,value){try{const db=await openDb();await new Promise((res,rej)=>{const tx=db.transaction('cache','readwrite');tx.objectStore('cache').put(value,key);tx.oncomplete=res;tx.onerror=()=>rej(tx.error)});db.close();return true}catch{return false}}
async function dbGet(key){try{const db=await openDb();const v=await new Promise((res,rej)=>{const tx=db.transaction('cache','readonly'),r=tx.objectStore('cache').get(key);r.onsuccess=()=>res(r.result);r.onerror=()=>rej(r.error)});db.close();return v}catch{return null}}
async function chooseWatchedFile(){if(!window.showOpenFilePicker){showToast('Vínculo automático disponível no Chrome/Edge do computador.');return}try{const[handle]=await window.showOpenFilePicker({multiple:false,types:[{description:'Planilha Excel',accept:{'application/vnd.ms-excel':['.xls'],'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet':['.xlsx','.xlsm']}}]});watchedFileHandle=handle;await dbPut('watchedFileHandle',handle);const file=await handle.getFile();watchedLastModified=file.lastModified||0;await dbPut('watchedLastModified',watchedLastModified);await processWorkbook(file);startFileWatcher();showToast('Planilha vinculada. Alterações serão verificadas automaticamente.')}catch(e){if(e?.name!=='AbortError')showToast('Não foi possível vincular a planilha.')}}
async function restoreWatchedFile(){if(!window.showOpenFilePicker)return;watchedFileHandle=await dbGet('watchedFileHandle');watchedLastModified=Number(await dbGet('watchedLastModified')||0);if(watchedFileHandle)startFileWatcher()}
function startFileWatcher(){clearInterval(fileWatchTimer);if(watchedFileHandle)fileWatchTimer=setInterval(checkWatchedFile,FILE_WATCH_INTERVAL_MS)}
async function checkWatchedFile(){if(!watchedFileHandle)return;try{if(watchedFileHandle.queryPermission){const p=await watchedFileHandle.queryPermission({mode:'read'});if(p!=='granted')return}const file=await watchedFileHandle.getFile();if((file.lastModified||0)>watchedLastModified){watchedLastModified=file.lastModified||Date.now();await dbPut('watchedLastModified',watchedLastModified);showToast('Alteração detectada. Atualizando...');await processWorkbook(file)}}catch(e){console.warn(e)}}
async function gzipToBase64(obj){const text=JSON.stringify(obj);if(typeof CompressionStream==='undefined')return{encoding:'plain',payload:text};const stream=new Blob([text]).stream().pipeThrough(new CompressionStream('gzip')),buf=await new Response(stream).arrayBuffer(),bytes=new Uint8Array(buf);let bin='';for(let i=0;i<bytes.length;i+=0x8000)bin+=String.fromCharCode(...bytes.subarray(i,i+0x8000));return{encoding:'gzip-base64',payload:btoa(bin)}}
async function decodeCloud(wrapper){if(wrapper.encoding==='plain')return JSON.parse(wrapper.payload);if(wrapper.encoding==='gzip-base64'){const bin=atob(wrapper.payload),bytes=new Uint8Array(bin.length);for(let i=0;i<bin.length;i++)bytes[i]=bin.charCodeAt(i);if(typeof DecompressionStream==='undefined')throw new Error('Este aparelho não suporta descompactação.');const stream=new Blob([bytes]).stream().pipeThrough(new DecompressionStream('gzip'));return JSON.parse(await new Response(stream).text())}throw new Error('Formato desconhecido')}
async function uploadCloud(){const cfg=getSettings();if(!cfg.apiUrl||!cfg.syncKey||!allTrees.length||!navigator.onLine)return;showToast('Enviando atualização para o celular...');try{const packed=await gzipToBase64({schemaVersion:6,updatedAt:currentUpdatedAt,source:currentSource,data:allTrees});await fetch(cfg.apiUrl,{method:'POST',mode:'no-cors',headers:{'Content-Type':'text/plain;charset=utf-8'},body:JSON.stringify({key:cfg.syncKey,updatedAt:currentUpdatedAt,encoding:packed.encoding,payload:packed.payload})});showToast('Dados enviados para sincronização.')}catch{showToast('Falha ao enviar para a nuvem.')}}
function jsonp(url,params){return new Promise((resolve,reject)=>{const cb='nobre_cb_'+Date.now()+'_'+Math.floor(Math.random()*99999),s=document.createElement('script'),tm=setTimeout(()=>{cleanup();reject(new Error('tempo esgotado'))},30000),cleanup=()=>{clearTimeout(tm);delete window[cb];s.remove()};window[cb]=d=>{cleanup();resolve(d)};const u=new URL(url);Object.entries({...params,callback:cb}).forEach(([k,v])=>u.searchParams.set(k,v));s.src=u.toString();s.onerror=()=>{cleanup();reject(new Error('erro de rede'))};document.body.appendChild(s)})}
async function refreshCloud(show=true){const cfg=getSettings();if(!cfg.apiUrl||!cfg.syncKey){if(show)showToast('Configure a URL do Apps Script e a chave.');return}if(!navigator.onLine){if(show)showToast('Sem internet. Usando a última versão salva no aparelho.');return}try{const r=await jsonp(cfg.apiUrl,{key:cfg.syncKey});if(!r?.ok||!r.payload){if(show)showToast('Ainda não há dados sincronizados nessa chave.');return}const remote=r.updatedAt?new Date(r.updatedAt).getTime():0,local=currentUpdatedAt?new Date(currentUpdatedAt).getTime():0;if(remote>local||!allTrees.length){const decoded=await decodeCloud({encoding:r.encoding,payload:r.payload});await setData(decoded.data,decoded.updatedAt,decoded.source||'Nuvem PC ↔ celular',true);await saveLocal(decoded);if(show)showToast('Dados atualizados pela nuvem.')}else if(show)showToast('Você já está com a versão mais recente.')}catch(e){console.warn(e);if(show)showToast('Não foi possível buscar a atualização.')}}
function startCloudTimer(){clearInterval(cloudTimer);const cfg=getSettings();if(cfg.apiUrl&&cfg.syncKey)cloudTimer=setInterval(()=>refreshCloud(false),CLOUD_INTERVAL_MS)}

function loadPreviewData(){
  if(!new URLSearchParams(location.search).has('preview')&&!window.__NOBRE_PREVIEW__)return false;
  const p=window.__NOBRE_INITIAL_DATA__;
  if(!p?.data?.length)return false;
  setData(p.data,p.updatedAt,p.source||'APP INVENTARIO(3).xls',true);
  return true;
}
async function loadBundledInitial(){const p=window.__NOBRE_INITIAL_DATA__;if(!p?.data?.length)return false;await setData(p.data,p.updatedAt,p.source||'APP INVENTARIO(3).xls',true);await saveLocal(p);return true}
function bindEvents(){window.addEventListener('online',()=>{updateNetwork();refreshCloud(false)});window.addEventListener('offline',updateNetwork);document.querySelectorAll('[data-go]').forEach(b=>b.onclick=()=>goScreen(b.dataset.go));$('syncBtn').onclick=()=>refreshCloud(true);$('navSync').onclick=()=>refreshCloud(true);$('settingsBtn').onclick=openSettings;$('navMore').onclick=openSettings;$('filterMapBtn').onclick=openFilters;$('filterTreesBtn').onclick=openFilters;$('applyFiltersBtn').onclick=()=>{$('filterDialog').close();currentPage=1;applyFilters()};$('clearFiltersBtn').onclick=e=>{e.preventDefault();clearFilters()};$('homeTreeSearch').addEventListener('input',renderHomeSearch);$('homeTreeClear').onclick=()=>{$('homeTreeSearch').value='';renderHomeSearch();$('homeTreeSearch').focus()};const ms=$('mapTreeSearch'),mb=$('mapTreeSearchBtn');if(mb)mb.onclick=findAndOpenMapTree;if(ms)ms.addEventListener('keydown',e=>{if(e.key==='Enter'){e.preventDefault();findAndOpenMapTree()}});const lb=$('labelsToggleBtn');if(lb)lb.onclick=toggleMapLabels;$('searchInput').addEventListener('input',()=>{currentPage=1;$('clearSearch').hidden=!$('searchInput').value;applyFilters()});$('clearSearch').onclick=()=>{$('searchInput').value='';$('clearSearch').hidden=true;currentPage=1;applyFilters()};$('statusChips').addEventListener('click',e=>{const b=e.target.closest('button');if(!b)return;document.querySelectorAll('#statusChips button').forEach(x=>x.classList.remove('active'));b.classList.add('active');$('statusFilter').value=b.dataset.status;currentPage=1;applyFilters()});$('prevPage').onclick=()=>{if(currentPage>1){currentPage--;renderTreeList();scrollTo(0,0)}};$('nextPage').onclick=()=>{const p=Math.ceil(filteredTrees.length/PAGE_SIZE);if(currentPage<p){currentPage++;renderTreeList();scrollTo(0,0)}};$('closeTreeDialog').onclick=()=>$('treeDialog').close();$('fitMapBtn').onclick=fitGeoMap;$('zoomIn').onclick=()=>zoomMap(1.35);$('zoomOut').onclick=()=>zoomMap(.74);$('myLocationBtn').onclick=centerGps;$('locateQuick').onclick=()=>{goScreen('map');setTimeout(centerGps,100)};$('stopNavBtn').onclick=stopOfflineNavigation;$('saveSettingsBtn').onclick=e=>{e.preventDefault();saveSettings()};$('refreshCloudBtn').onclick=e=>{e.preventDefault();refreshCloud(true)};$('watchFileBtn').onclick=e=>{e.preventDefault();chooseWatchedFile()};const importIt=e=>{e?.preventDefault();$('excelFile').click()};$('importBtn').onclick=importIt;$('importQuick').onclick=importIt;$('excelFile').addEventListener('change',async e=>{const f=e.target.files?.[0];if(!f)return;try{await processWorkbook(f)}catch(err){console.error(err);showToast('Erro ao ler a planilha: '+err.message)}finally{e.target.value=''}})}
async function boot(){bindEvents();updateNetwork();updateLabelsToggle();initGeoMap();if(loadPreviewData())return;const local=await loadLocal();if(local?.schemaVersion>=2&&local?.data?.length)await setData(local.data,local.updatedAt,local.source,false);else await loadBundledInitial();await restoreWatchedFile();startCloudTimer();const cfg=getSettings();if(cfg.apiUrl&&cfg.syncKey&&navigator.onLine)refreshCloud(false);renderKpis();updateLastWorkbookInfo();if(/Android/i.test(navigator.userAgent)||new URLSearchParams(location.search).has('android')){goScreen('map');setTimeout(()=>resizeMap(true),120)}}
boot();
